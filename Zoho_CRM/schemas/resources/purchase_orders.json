{
    "parameters": [
        {
            "meta": {
                "validation": {
                    "required": true
                },
                "description": "The ID of the purchase order to retrieve.",
                "displayName": "Purchase Order ID",
                "displayType": "text",
                "placeholder": "Enter the purchase order ID"
            },
            "name": "purchase_order_id"
        }
    ],
    "definition": {
        "url": "https://www.zohoapis.in/crm/v2/Purchase_Orders/{{parameters.purchase_order_id}}",
        "method": "get",
        "headers": {
            "Authorization": "Bearer {{secrets.access_token}}"
        },
        "response": {
            "error": {
                "code": "{{response.status}}",
                "message": "{{response.data}}"
            },
            "output": "{{response.data}}"
        }
    }
}

{
    "parameters": [
        {
            "meta": {
                "validation": {
                    "required": true
                },
                "description": "The rate ID to check against.",
                "displayName": "Rate ID",
                "displayType": "number",
                "placeholder": "Enter the rate ID"
            },
            "name": "rateId"
        }
    ],
    "getTaxRates": {
        "definition": {
            "url": "https://{{secrets.domain}}/rest/default/V1/taxRates",
            "method": "get",
            "headers": {
                "Authorization": "Bearer {{secrets.access_token}}"
            },
            "response": {
                "error": {
                    "code": "{{response.status}}",
                    "message": "{{response.data.message}}"
                },
                "output": "{{response.data}}"
            }
        },
        "parameters": []
    },
    "definition": {
        "url": "https://{{secrets.domain}}/rest/default/V1/taxRates/{{parameters.rateId}}",
        "method": "get",
        "headers": {
            "Authorization": "Bearer {{secrets.access_token}}"
        },
        "response": {
            "error": {
                "code": "{{response.status}}",
                "message": "{{response.data.message}}"
            },
            "output": "{{response.data}}"
        }
    }
}

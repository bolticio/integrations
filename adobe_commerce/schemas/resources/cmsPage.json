{
    "parameters": [
        {
            "meta": {
                "validation": {
                    "required": true
                },
                "description": "The ID of the CMS page to delete.",
                "displayName": "Page ID",
                "displayType": "text",
                "placeholder": "Enter the Page ID"
            },
            "name": "id"
        }
    ],
    "definition": {
        "url": "https://{{secrets.domain}}/rest/default/V1/cmsPage/{{parameters.id}}",
        "method": "delete",
        "headers": {
            "Authorization": "Bearer {{secrets.access_token}}"
        },
        "response": {
            "error": {
                "code": "{{response.status}}",
                "message": "{{response.data.message}}"
            },
            "output": "{{response.data}}"
        }
    }
}

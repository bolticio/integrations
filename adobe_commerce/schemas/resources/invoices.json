{
    "parameters": [
        {
            "meta": {
                "validation": {
                    "required": true
                },
                "description": "The ID of the invoice to add a comment to.",
                "displayName": "Entity ID",
                "displayType": "text",
                "placeholder": "Enter the entity ID"
            },
            "name": "id"
        }
    ],
    "definition": {
        "url": "https://{{secrets.domain}}/rest/default/V1/invoices/{{parameters.id}}/comments",
        "method": "get",
        "headers": {
            "Authorization": "Bearer {{secrets.access_token}}"
        },
        "response": {
            "error": {
                "code": "{{response.status}}",
                "message": "{{response.data.message}}"
            },
            "output": "{{response.data}}"
        }
    }
}

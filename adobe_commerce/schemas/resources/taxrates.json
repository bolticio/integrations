{
    "parameters": [
        {
            "meta": {
                "options": [
                    {
                        "label": "Search Tax Rates",
                        "value": "taxRates.searchTaxRates",
                        "description": "Search for specific tax rates."
                    },
                    {
                        "label": "Get Tax Rate by ID",
                        "value": "taxRates.getTaxRateById",
                        "description": "Retrieve a specific tax rate by its ID."
                    }
                ],
                "validation": {
                    "required": true
                },
                "description": "Select the operation you want to perform.",
                "displayName": "Operation",
                "displayType": "select",
                "placeholder": "Select an operation",
                "dependencies": {
                    "conditions": [
                        {
                            "field": "resource",
                            "value": "taxRates",
                            "operator": "EQUALS"
                        }
                    ]
                }
            },
            "name": "operation"
        }
    ],
    "getTaxRates": {
        "definition": {
            "url": "https://{{secrets.domain}}/rest/default/V1/taxRates",
            "method": "get",
            "headers": {
                "Authorization": "Bearer {{secrets.access_token}}"
            },
            "response": {
                "error": {
                    "code": "{{response.status}}",
                    "message": "{{response.data.message}}"
                },
                "output": "{{response.data}}"
            }
        },
        "parameters": []
    },
    "searchtaxrates": {
        "definition": {
            "qs": {
                "searchCriteria[pageSize]": "{{parameters.pageSize}}"
            },
            "url": "https://{{secrets.domain}}/rest/default/V1/taxRates/search",
            "method": "get",
            "headers": {
                "Authorization": "Bearer {{secrets.access_token}}"
            },
            "response": {
                "error": {
                    "code": "{{response.status}}",
                    "message": "{{response.data.message}}"
                },
                "output": "{{response.data}}"
            }
        },
        "parameters": [
            {
                "meta": {
                    "value": 1,
                    "validation": {
                        "required": false
                    },
                    "description": "The number of products to retrieve per page.",
                    "displayName": "Page Size",
                    "displayType": "number",
                    "placeholder": "Enter the number of products per page"
                },
                "name": "pageSize"
            }
        ]
    },
    "gettaxratebyid": {
        "definition": {
            "url": "https://{{secrets.domain}}/rest/default/V1/taxRates/{{parameters.rateId}}",
            "method": "get",
            "headers": {
                "Authorization": "Bearer {{secrets.access_token}}"
            },
            "response": {
                "error": {
                    "code": "{{response.status}}",
                    "message": "{{response.data.message}}"
                },
                "output": "{{response.data}}"
            }
        },
        "parameters": [
            {
                "meta": {
                    "validation": {
                        "required": true
                    },
                    "description": "The rate ID to check against.",
                    "displayName": "Rate ID",
                    "displayType": "number",
                    "placeholder": "Enter the rate ID"
                },
                "name": "rateId"
            }
        ]
    }
}

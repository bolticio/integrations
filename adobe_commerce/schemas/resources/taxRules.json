{
    "parameters": [
        {
            "meta": {
                "value": 1,
                "validation": {
                    "required": false
                },
                "description": "The number of products to retrieve per page.",
                "displayName": "Page Size",
                "displayType": "number",
                "placeholder": "Enter the number of products per page"
            },
            "name": "pageSize"
        }
    ],
    "getTaxRules": {
        "definition": {
            "qs": {
                "searchCriteria[pageSize]": "{{parameters.pageSize}}"
            },
            "url": "https://{{secrets.domain}}/rest/default/V1/taxRules/search",
            "method": "get",
            "headers": {
                "Authorization": "Bearer {{secrets.access_token}}"
            },
            "response": {
                "error": {
                    "code": "{{response.status}}",
                    "message": "{{response.data.message}}"
                },
                "output": "{{response.data}}"
            }
        },
        "parameters": [
            {
                "meta": {
                    "value": 1,
                    "validation": {
                        "required": false
                    },
                    "description": "The number of products to retrieve per page.",
                    "displayName": "Page Size",
                    "displayType": "number",
                    "placeholder": "Enter the number of products per page"
                },
                "name": "pageSize"
            }
        ]
    },
    "definition": {
        "qs": {
            "searchCriteria[pageSize]": "{{parameters.pageSize}}"
        },
        "url": "https://{{secrets.domain}}/rest/default/V1/taxRules/search",
        "method": "get",
        "headers": {
            "Authorization": "Bearer {{secrets.access_token}}"
        },
        "response": {
            "error": {
                "code": "{{response.status}}",
                "message": "{{response.data.message}}"
            },
            "output": "{{response.data}}"
        }
    }
}

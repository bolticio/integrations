{
    "parameters": [
        {
            "meta": {
                "options": [
                    {
                        "label": "Get Store Configurations",
                        "value": "store.getStoreConfigs",
                        "description": "Retrieve the store configurations."
                    },
                    {
                        "label": "Get Store Groups",
                        "value": "store.getStoreGroups",
                        "description": "Retrieve the store groups."
                    },
                    {
                        "label": "Get Store Views",
                        "value": "store.getStoreViews",
                        "description": "Retrieve the store views."
                    },
                    {
                        "label": "Get Websites",
                        "value": "store.getWebsites",
                        "description": "Retrieve the websites associated with the store."
                    }
                ],
                "validation": {
                    "required": true
                },
                "description": "Select the operation you want to perform.",
                "displayName": "Operation",
                "displayType": "select",
                "placeholder": "Select an operation",
                "dependencies": {
                    "conditions": [
                        {
                            "field": "resource",
                            "value": "store",
                            "operator": "EQUALS"
                        }
                    ]
                }
            },
            "name": "operation"
        }
    ],
    "getstoreconfigs": {
        "definition": {
            "url": "https://{{secrets.domain}}/rest/default/V1/store/storeConfigs",
            "method": "get",
            "headers": {
                "Authorization": "Bearer {{secrets.access_token}}"
            },
            "response": {
                "error": {
                    "code": "{{response.status}}",
                    "message": "{{response.data.message}}"
                },
                "output": "{{response.data}}"
            }
        },
        "parameters": []
    },
    "getstoregroups": {
        "definition": {
            "url": "https://{{secrets.domain}}/rest/default/V1/store/storeGroups",
            "method": "get",
            "headers": {
                "Authorization": "Bearer {{secrets.access_token}}"
            },
            "response": {
                "error": {
                    "code": "{{response.status}}",
                    "message": "{{response.data.message}}"
                },
                "output": "{{response.data}}"
            }
        },
        "parameters": []
    },
    "getstoreviews": {
        "definition": {
            "url": "https://{{secrets.domain}}/rest/default/V1/store/storeViews",
            "method": "get",
            "headers": {
                "Authorization": "Bearer {{secrets.access_token}}"
            },
            "response": {
                "error": {
                    "code": "{{response.status}}",
                    "message": "{{response.data.message}}"
                },
                "output": "{{response.data}}"
            }
        },
        "parameters": []
    },
    "getwebsites": {
        "definition": {
            "url": "https://{{secrets.domain}}/rest/default/V1/store/websites",
            "method": "get",
            "headers": {
                "Authorization": "Bearer {{secrets.access_token}}"
            },
            "response": {
                "error": {
                    "code": "{{response.status}}",
                    "message": "{{response.data.message}}"
                },
                "output": "{{response.data}}"
            }
        },
        "parameters": []
    }
}

{
    "parameters": [],
    "definition": {
        "url": "https://{{secrets.domain}}/rest/default/V1/attributeMetadata/customer",
        "method": "get",
        "headers": {
            "Authorization": "Bearer {{secrets.access_token}}"
        },
        "response": {
            "error": {
                "code": "{{response.status}}",
                "message": "{{response.data.message}}"
            },
            "output": "{{response.data}}"
        }
    }
}

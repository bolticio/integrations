{
    "api_key": {
        "validate": {
            "url": "https://translation.googleapis.com/language/translate/v2/languages?key={{secrets.api_key}}",
            "method": "get",
            "response": {
                "error": {
                    "code": "{{response.data.error.code}}",
                    "message": "{{response.data.error.message}}"
                },
                "output": "{{response.data.data}}"
            }
        },
        "parameters": [
            {
                "meta": {
                    "validation": {
                        "required": true,
                        "requiredDetail": {
                            "errorMsg": "API Key is required field"
                        }
                    },
                    "description": "Refer to <a href='https://cloud.google.com/docs/authentication/api-keys' target='_blank' rel='noopener'><span>Google Cloud API Key documentation</span></a>",
                    "displayName": "API Key",
                    "displayType": "password",
                    "placeholder": "Enter API Key"
                },
                "name": "api_key",
                "dependencies": {
                    "conditions": [
                        {
                            "field": "type",
                            "value": "api_key",
                            "operator": "EQUALS"
                        }
                    ]
                }
            }
        ]
    },
    "parameters": [
        {
            "meta": {
                "value": "api_key",
                "options": [
                    {
                        "label": "API Key",
                        "value": "api_key"
                    }
                ],
                "validation": {
                    "required": true
                },
                "description": "Choose the type of authentication you want to use.",
                "displayName": "Authentication Type",
                "displayType": "hidden",
                "placeholder": "Select an authentication type"
            },
            "name": "type"
        }
    ]
}
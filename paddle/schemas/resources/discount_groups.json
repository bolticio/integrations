{
    "parameters": [
        {
            "meta": {
                "validation": {
                    "required": false
                },
                "description": "Return only the IDs specified. Use a comma-separated list to get multiple entities.",
                "displayName": "Discount Group ID",
                "displayType": "text",
                "placeholder": "Enter the discount group ID or comma-separated list."
            },
            "name": "id"
        },
        {
            "meta": {
                "options": [
                    {
                        "label": "Created At (ascending)",
                        "value": "created_at[ASC]"
                    },
                    {
                        "label": "Created At (descending)",
                        "value": "created_at[DESC]"
                    },
                    {
                        "label": "ID (ascending)",
                        "value": "id[ASC]"
                    },
                    {
                        "label": "ID (descending)",
                        "value": "id[DESC]"
                    }
                ],
                "htmlProps": {
                    "allowDynamic": true
                },
                "validation": {
                    "required": false
                },
                "description": "Order returned entities by the specified field and direction ([ASC] or [DESC]). For example, ?order_by=id[ASC].",
                "displayName": "Order By",
                "displayType": "select",
                "placeholder": "Select a response entity"
            },
            "name": "order_by"
        }
    ],
    "definition": {
        "qs": {
            "id": "{{parameters.id}}",
            "order_by": "{{parameters.order_by}}"
        },
        "url": "https://{{secrets.environment_url}}/discount-groups",
        "method": "get",
        "headers": {
            "Accept": "application/json",
            "Authorization": "Bearer {{secrets.api_key}}"
        },
        "response": {
            "error": {
                "code": "{{response.status}}",
                "message": "{{response.data}}"
            },
            "output": "{{response.data.data}}"
        },
        "pagination": {
            "qs": {
                "per_page": 200
            },
            "type": "url",
            "default": {
                "next_url": null,
                "per_page": 200
            },
            "next_url": "{{response.data.meta.pagination.next}}",
            "condition": "{{response.data.meta.pagination.has_more}} !== false"
        }
    }
}

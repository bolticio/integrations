{
    "api_key": {
        "validate": {
            "url": "https://{{secrets.environment_url}}/events",
            "method": "get",
            "headers": {
                "Authorization": "Bearer {{secrets.api_key}}"
            },
            "response": {
                "error": {
                    "code": "{{response.status}}",
                    "message": "{{response.data}}. Refer to the Paddle docs: {{response.data.error.documentation_url}}."
                },
                "output": "{{response.data}}"
            }
        },
        "parameters": [
            {
                "meta": {
                    "options": [
                        {
                            "label": "Sandbox",
                            "value": "sandbox-api.paddle.com"
                        },
                        {
                            "label": "Production",
                            "value": "api.paddle.com"
                        }
                    ],
                    "validation": {
                        "required": true,
                        "requiredDetail": {
                            "errorMsg": "Environment is required"
                        }
                    },
                    "description": "Choose an environment for hitting Paddle APIs.",
                    "displayName": "Environment",
                    "displayType": "select",
                    "placeholder": "Select a paddle account environment"
                },
                "name": "environment_url"
            },
            {
                "meta": {
                    "validation": {
                        "required": true,
                        "requiredDetail": {
                            "errorMsg": "API key is required"
                        }
                    },
                    "description": "Refer to the <a href='https://developer.paddle.com/api-reference/about/authentication' target='blank'>Paddle authentication documentation</a> for detailed instructions on getting your API key.",
                    "displayName": "API key",
                    "displayType": "password",
                    "placeholder": "Enter API key"
                },
                "name": "api_key"
            }
        ]
    },
    "parameters": [
        {
            "meta": {
                "value": "api_key",
                "options": [
                    {
                        "label": "API key",
                        "value": "api_key"
                    }
                ],
                "displayName": "Authentication Type",
                "displayType": "hidden"
            },
            "name": "type"
        }
    ]
}
{
    "parameters": [
        {
            "meta": {
                "options": [
                    {
                        "label": "Access Token",
                        "value": "access_token"
                    }
                ],
                "validation": {
                    "required": true,
                    "requiredDetail": {
                        "errorMsg": "Authentication Type is a required field"
                    }
                },
                "description": "Choose the type of authentication you want to use.",
                "displayName": "Authentication Type",
                "displayType": "select",
                "placeholder": "Select an authentication type"
            },
            "name": "type"
        }
    ],
    "access_token": {
        "validate": {
            "url": "https://dev.azure.com/{{secrets.organization_id}}/_apis/projects?api-version=6.0",
            "method": "get",
            "headers": {
                "Authorization": "Basic convertBufferBase64String('':{{secrets.access_token}})"
            },
            "response": {
                "error": {
                    "code": "{{response.status}}",
                    "message": "Authorization failed"
                },
                "output": "{{response.data}}"
            }
        },
        "parameters": [
            {
                "meta": {
                    "validation": {
                        "required": true,
                        "requiredDetail": {
                            "errorMsg": "Acess Token is a required field"
                        }
                    },
                    "description": "Refer to the <a href='https://docs.microsoft.com/en-us/azure/devops/pipelines/release/azure-rm-endpoint' target='_blank'>Azure DevOps authentication documentation</a> for detailed instructions on getting your API keys.",
                    "displayName": "Personal Access Token",
                    "displayType": "password",
                    "placeholder": "",
                    "dependencies": {
                        "conditions": [
                            {
                                "field": "type",
                                "value": "access_token",
                                "operator": "EQUALS"
                            }
                        ]
                    }
                },
                "name": "access_token"
            },
            {
                "meta": {
                    "validation": {
                        "required": true,
                        "requiredDetail": {
                            "errorMsg": "Organization Type is a required field"
                        }
                    },
                    "displayName": "Organization Name",
                    "displayType": "text",
                    "placeholder": "",
                    "dependencies": {
                        "conditions": [
                            {
                                "field": "type",
                                "value": "access_token",
                                "operator": "EQUALS"
                            }
                        ]
                    }
                },
                "name": "organization_id"
            }
        ]
    }
}
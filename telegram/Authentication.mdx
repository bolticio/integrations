{
    "api_key": {
        "validate": {
            "url": "https://api.telegram.org/bot{{secrets.api_token}}/getMe",
            "method": "get",
            "headers": {
                "Content-Type": "application/json"
            },
            "response": {
                "error": {
                    "code": "{{response.error_code}}",
                    "message": "{{response.description}}"
                },
                "output": "{{response.data}}"
            }
        },
        "parameters": [
            {
                "meta": {
                    "validation": {
                        "required": true,
                        "requiredDetail": {
                            "errorMsg": "Bot API token is required."
                        }
                    },
                    "description": "To learn how to create and set up a bot, please consult our Introduction to <a href='https://core.telegram.org/bots/tutorial#obtain-your-bot-token' target='_blank'> Obtain your Bot tokens.</a>",
                    "displayName": "Bot API Token",
                    "displayType": "password",
                    "placeholder": "Enter Bot API token",
                    "dependencies": {
                        "conditions": [
                            {
                                "field": "type",
                                "value": "api_key",
                                "operator": "EQUALS"
                            }
                        ]
                    }
                },
                "name": "api_token"
            }
        ]
    },
    "parameters": [
        {
            "meta": {
                "value": "api_key",
                "options": [
                    {
                        "label": "API Token",
                        "value": "api_key"
                    }
                ],
                "validation": {
                    "required": true,
                    "requiredDetail": {
                        "errorMsg": "Please select the Authentication"
                    }
                },
                "description": "Choose the type of authentication.",
                "displayName": "Authentication Type",
                "displayType": "select",
                "placeholder": "Select an authentication type"
            },
            "name": "type"
        }
    ]
}
# Pipedrive

Pipedrive is a sales CRM platform that helps teams manage leads, deals, contacts, organizations, and activities. The Pipedrive node in Boltic lets you connect to your Pipedrive data to sync entities, power analytics, and automate workflows through a comprehensive set of operations designed for seamless sales process management.

## Pipedrive Account

- Select the Pipedrive account you want to connect.
- If you don't have an existing account, please add one using the "Add New Account" option before proceeding.

## Available Resource

Here are some of the available resource you can use with the **Pipedrive** node:

### Resource
- **Webhook**: A webhook is a real-time HTTP callback that posts event data to your URL when specified actions happen in Pipedrive, so your system can react instantly.

## Basic Operations

Here are some of the basic operations you can perform with the Pipedrive node:

### Activity Event
- **Create Activity**: Trigger events when activities are created.
- **Change Activity**: Trigger events when activities are updated.
- **Delete Activity**: Trigger events when activities are deleted.
- **All Activity Events**: Trigger events for all activity actions.

### Deal Event
- **Create Deal**: Trigger events when deals are created.
- **Change Deal**: Trigger events when deals are updated.
- **Delete Deal**: Trigger events when deals are deleted.
- **All Deal Events**: Trigger events for all deal actions.

### Lead Event
- **Create Lead**: Trigger events when leads are created.
- **Change Lead**: Trigger events when leads are updated.
- **Delete Lead**: Trigger events when leads are deleted.
- **All Lead Events**: Trigger events for all lead actions.

### Note Event
- **Create Note**: Trigger events when notes are created.
- **Change Note**: Trigger events when notes are updated.
- **Delete Note**: Trigger events when notes are deleted.
- **All Note Events**: Trigger events for all note actions.

### Organization Event
- **Create Organization**: Trigger events when organizations are created.
- **Change Organization**: Trigger events when organizations are updated.
- **Delete Organization**: Trigger events when organizations are deleted.
- **All Organization Events**: Trigger events for all organization actions.

### Person Event
- **Create Person**: Trigger events when people are created.
- **Change Person**: Trigger events when people are updated.
- **Delete Person**: Trigger events when people are deleted.
- **All Person Events**: Trigger events for all person actions.

### Pipeline Event
- **Create Pipeline**: Trigger events when pipelines are created.
- **Change Pipeline**: Trigger events when pipelines are updated.
- **Delete Pipeline**: Trigger events when pipelines are deleted.
- **All Pipeline Events**: Trigger events for all pipeline actions.

### Product Event
- **Create Product**: Trigger events when products are created.
- **Change Product**: Trigger events when products are updated.
- **Delete Product**: Trigger events when products are deleted.
- **All Product Events**: Trigger events for all product actions.

### Stage Event
- **Create Stage**: Trigger events when stages are created.
- **Change Stage**: Trigger events when stages are updated.
- **Delete Stage**: Trigger events when stages are deleted.
- **All Stage Events**: Trigger events for all stage actions.

### User Event
- **Create User**: Trigger events when users are created.
- **Change User**: Trigger events when users are updated.
- **Delete User**: Trigger events when users are deleted.
- **All User Events**: Trigger events for all user actions.

## Conclusion

Pipedrive makes CRM integration easy, secure, and scalable—enabling teams to centralize sales data, drive pipeline visibility, and automate sales workflows within your applications.

If you need further assistance, please contact Boltic support <a href="mailto:support@boltic.io">support@boltic.io</a>
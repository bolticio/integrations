{
    "api_key": {
        "validate": {
            "url": "https://api.intercom.io/me",
            "method": "get",
            "headers": {
                "Accept": "application/json",
                "Authorization": "Bearer {{secrets.api_key}}"
            },
            "response": {
                "error": {
                    "code": "{{response.status}}",
                    "message": "{{response.data.message}}"
                },
                "output": "{{response.data}}"
            }
        },
        "parameters": [
            {
                "meta": {
                    "validation": {
                        "required": true,
                        "requiredDetail": {
                            "errorMsg": "API Key is required"
                        }
                    },
                    "description": "Refer to the <a href='https://docs.boltic.io/docs/integrations/Intercom/' target='_blank' rel='noopener'>Boltic Integration Builder documentation</a> for detailed instructions on getting your API key.",
                    "displayName": "API Key/Token",
                    "displayType": "password",
                    "placeholder": "Enter API Token",
                    "dependencies": {
                        "conditions": [
                            {
                                "field": "type",
                                "value": "api_key",
                                "operator": "EQUALS"
                            }
                        ]
                    }
                },
                "name": "api_key"
            }
        ]
    },
    "parameters": [
        {
            "meta": {
                "value": "api_key",
                "options": [
                    {
                        "label": "API Token",
                        "value": "api_key"
                    }
                ],
                "displayName": "Authentication Type",
                "displayType": "hidden"
            },
            "name": "type"
        }
    ]
}